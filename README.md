# 服饰推荐规则关系原型 V1.2

这是一个可运行的逻辑原型，用来验证：用户输入具体改变哪些穿搭结果、多条关系怎样共同生成 2-3 个完整穿着方案，以及规则修改后如何同步影响案例运行结果。

## 两个页面

- `index.html`：案例运行。选择温度、场合、整体色彩、身材情况、脸型、风格、正式程度、潮流程度、当次目标和明确边界，直接查看“本次输入 → 受影响的穿搭结果 → 完整方案”。
- `rules.html`：规则管理。默认使用单一影响关系矩阵展示“输入分组 → 输入字段”与穿着框架、服装样式、颜色搭配三组结果字段的覆盖关系；横向和纵向都采用“大类 + 子类”两层结构，色阶表示直接、间接或条件影响，点击色块进入具体关系配置。配置页只编辑当前选中的一条关系，通过按钮导航切换关系，不再重复展示业务关系列表。每条关系的条件字段和结果字段都受范围约束，输入选项、服装库、配色和潮流方向放在基础资料中维护。

两个页面共用 `rules-data.js` 和 `rule-engine.js`，没有另一套只用于展示的推荐逻辑。

## 本地访问

静态服务地址：

- 案例运行：`http://127.0.0.1:4177/index.html`
- 规则管理：`http://127.0.0.1:4177/rules.html`

无需启动服务时，可直接打开单文件版：

- `garment-recommendation-standalone.html`

单文件版已内嵌样式、规则数据、推荐引擎和案例交互。更新原型源文件后可重新生成：

```powershell
node build-standalone.js
```

如需重新启动服务：

```powershell
node serve-prototype.js
```

原型不依赖后端。草稿、发布版本和最近一次演示输入保存在浏览器 `localStorage`。

## 当前逻辑

- 温度是近期最低至最高温度的范围，决定层数、袖长、外层、覆盖和材质厚薄。
- 身材情况用身材比例和 H/O/X/A/Y 轮廓沟通；脸型单独输入，只影响领口和脸部周边细节。
- 场合定义使用边界；风格定义款式语言；正式程度定义服装完成度；潮流程度定义版型比例和细节强度。四者独立输入，再共同约束完整搭配。
- 风格切换会改变具体上装、外层、下装、整体轮廓、款式细节和图案纹理，不是只改方案名称。
- 颜色方案直接绑定上装、外层、下装或点缀位置，不输出与服装分离的色卡结论。
- 同一色温、明度对比和彩度可以配置多条配色路线，使三个候选既有款式差异，也有配色差异；候选不得超出本次分析出的对比和彩度。
- 潮流不再使用固定的高低等级，而是动态维护的潮流方向。每个方向包含理念来源、适用时间、服装路线、廓形比例、材质和细节映射。
- 表达强度只有在选定潮流方向后出现；切换潮流方向必须改变具体服装或版型内容，不能只改变说明文案。
- 腰线、线条和比例属于服装版型属性，不是独立推荐模块。
- 预计上身效果由服装款式、版型比例、细节强度和颜色关系共同生成，只作结果解释，不作为单独配置对象。
- 页面不展示内部连续计算值；案例页的分析模块只保留紧凑的“输入胶囊 → 结果胶囊”。
- 规则主表固定为场景条件、个人特征、风格偏好、本次目标与边界四个输入分组，并在第二层展示近期温度、使用场合、外观色彩、身材情况、脸型、风格方向、正式程度、潮流方向、本次调整目标、拒绝与边界 10 个输入字段。
- 同一类条件在编辑器内管理，例如近期温度包含 6 个匹配分支，脸型包含 6 个领口分支，不在主列表重复铺开。
- 规则管理分为“规则总览”和“规则配置”两个工作模式；总览单元格可直接跳转到对应配置。总览只回答“输入影响哪些结果”，配置页只维护当前关系的条件、影响结果、具体处理和理由；关系导航使用按钮，且脸型配置只显示脸型条件与领口结果，避免跨模块误配。
- 多个输入的中间计算收在“计算方式”中，默认折叠，不作为新的顶层规则。
- 每条关系明确影响范围；未列入范围的服装属性不会因为这条关系改变。
- 每次输出 2-3 个完整候选，并提供本套如何落实、未验证项、验证方式和失败条件。

## 边界

- 当前效果决策不使用湿度、室内外、活动强度、时长、光线、角色和观察者。
- 预算、品牌、新旧款、库存和护理条件属于商品匹配层，不进入当前效果路线。
- 不使用置信度决定是否出结果；缺少外观或身体事实时标记“需验证”。
- 当前不执行图片识别。正式接入图片后，识别结果仍需用户确认，再作为输入事实。
- 商品尺寸、纸样、面料厚度、织法、垂坠度和蓬胀度会改变实际效果，因此方案是可验证的预计结果，不是试穿结论。

## 验证

```powershell
node verify-prototype.js
```

自动验收覆盖：顶部导航对齐、紧凑输入结果关系、温度和脸型联动、潮流真实服装变化、三套配色差异、两层表头影响矩阵、色阶关联标记、总览到配置跳转、潮流资料修改、规则发布与跨页面同步，以及 1440×900 和 390×844 视口。

## 规格文档

- [穿搭推荐系统完整优化方案 V2.0](../../garment-recommendation-system-optimization-plan-v2.0-20260815.md)
- [规则工作台与动态潮流 V1.2](../../garment-rule-workbench-trend-v1.2-20260814.md)
- [规则关系原型 V1.1](../../garment-rule-relationship-prototype-v1.1-20260814.md)
- [规则关系原型 V1.0](../../garment-rule-relationship-prototype-v1.0-20260814.md)
- [输入输出与原型优化方案 V0.9](../../garment-input-output-prototype-v0.9-20260813.md)
- [可编辑规则系统 V0.7](../../garment-editable-rule-system-v0.7-20260813.md)
